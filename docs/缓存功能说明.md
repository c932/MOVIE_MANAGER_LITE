# 缓存功能说明

## 1. 缓存目的

缓存用于减少重复扫描与重复解析，提高启动和筛选速度。

## 2. 缓存文件

电影缓存文件：

- `data/movie_cache.json`

## 3. 生效逻辑

1. 首次扫描：生成缓存。
2. 后续扫描：优先使用缓存并增量更新。
3. 强制重扫：忽略缓存并重建。

## 4. 手动清理

如需完全重建缓存，可删除：

- `data/movie_cache.json`

下次扫描会自动重新生成。

## 5. 注意事项

1. 缓存仅保存可重建数据，不影响原始媒体文件。
2. 删除缓存不会删除电影内容。
